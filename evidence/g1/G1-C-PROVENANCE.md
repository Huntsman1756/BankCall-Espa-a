# G1-C Code-to-Evidence Provenance

Audit of the chain `scripts/g1/taxonomy_g1c.py` → `evidence/g1/g1-c-evidence.json`,
triggered by the observation that the script file was last written at
2026-09-13 21:25:50 CEST, ~77 s *after* the evidence was generated at
2026-09-13T19:24:33Z (21:24:33 CEST).

## SHAs

- Published script (`bd54457`, = current): sha256
  `c4594b8337953d1892bb827317eea45cc7abbdb085dae69be5d42752e9b53814`
- Executed script (produced the 19:24:33Z evidence): sha256 **unrecoverable**.
  The file was never committed/staged before `bd54457`; no `.pyc` exists
  (scripts executed directly are not cached); no editor history, shadow copy,
  or session transcript captured its full bytes. A complete 18:56 CEST snapshot
  (305 lines, 13069 bytes) survives in a tool-output capture and was used for
  the forensic reconstruction below.

## Forensic reconstruction (executed vs published)

Timeline recovered from tool-output captures:

- 18:10 file created; 18:56 full rewrite (captured, 305 lines).
- 21:12:08Z run A wrote evidence; 21:12:26–21:24:34 run B (determinism
  re-execution) wrote the committed evidence at 19:24:33Z.
- 21:25:13–21:25:29 run A↔B comparison: all outputs identical except
  `generated_at` and the six `versioning_diffs.*.report_sha256` values —
  the executed code hashed each Arelle report with plain sha256, which
  includes Arelle's per-run `Generated by Arelle ... at <ts>` comment.
- 21:25:50 post-run patch introduced `_report_hash()` (strips that comment)
  and the PYTHONHASHSEED docstring note, fixing the residual nondeterminism.
- 21:26 a third run was started with the patched script and never completed
  (the >250 min process later observed and interrupted).

Established properties of the executed version, proven by the evidence it
wrote (not by source recovery):

- contained `ns_renames` / `role_changes` extraction, `ModelVersReport(mx_t)`
  API usage, `classify_concepts(..., ns_map, role_map, ns_pairs)`, `_jsonable`
  serialization, and the `qname_space_normalization` notes — all present as
  fields/text in the committed evidence;
- used plain sha256 for `report_sha256` — committed values equal plain sha256
  of the report files, not the timestamp-stripped digest.

## Difference classification

| Difference (executed → published) | Class | Alters outputs? |
|---|---|---|
| `_report_hash`: plain sha256 → timestamp-stripped sha256 | EVIDENCE_METADATA_ONLY | only `report_sha256` fields in evidence |
| Docstring: PYTHONHASHSEED determinism note | COMMENTS/LOGGING_ONLY | no |
| Possible residual cosmetic deltas | UNKNOWN (unprovable, output-neutral) | no |

No difference alters fingerprints, versioning-report content, concept
classification, or gate logic outcomes.

## Verification re-execution (published code)

`PYTHONHASHSEED=0 python scripts/g1/taxonomy_g1c.py` completed in ~5 min
(Arelle 2.44.0, Python 3.11.15, offline frozen mirror). Field-level diff of
regenerated vs committed evidence:

- `concept-mapping.json`: byte-identical.
- `dts-fingerprints.json`: `fingerprints_sha256` identical.
- versioning XMLs: identical except line-2 Arelle generated-at comment.
- `g1-c-evidence.json`: identical except `generated_at` and the six
  `report_sha256` values, which now hold the deterministic stripped digests.
- Gates: all three PASS.

Conclusion: the published script reproduces the committed evidence exactly,
modulo the intentional `report_sha256` determinism fix. G1-C closure is
demonstrated by `bd54457` plus this verification record.

## Historical artifacts

- Pre-patch evidence (executed-version output, plain report_sha256):
  `evidence/g1/historical/g1-c-evidence-20260913T192433Z-prepatch.json`
  (also immutable in git at `bd54457`).
- Run-A outputs (first determinism run): `%TEMP%\g1c_run2\` — local only.

## Post-closure relocation

The six Arelle versioning reports were moved `g0_acquisition/versioning/` →
`evidence/g1/versioning/` (content unchanged; digests preserved). `report_path`
values inside `g1-c-evidence.json` and the historical copy still record the
pre-relocation location — they are run-generated output and were not edited.
The bulk `dts_mirror/` stays under `g0_acquisition/` by design: it is frozen
G0-acquired source data, and every resolver script derives `MIRROR` from it.
